<%= simple_form_for @visit_status, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@visit_status[:name]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                visit_statuses_path, :class => 'btn btn-default' %>
<% end %>
